
BASHLOCAL="$BASHDIR/machines/"
SHORTHOST=$( hostname -s )

LOCALSPEC="$BASHLOCAL/$SHORTHOST"

if [ ! -r "$LOCALSPEC" ] ; then
	LOCALSPEC="$BASHLOCAL/DEFAULT"
fi

[ -r "$LOCALSPEC" ] && source "$LOCALSPEC"

# vim: ft=sh
